#include <bits/stdc++.h>
using namespace std;
int main() {
	int t;
	cin>>t;
	while(t--){
	    int a, b, c, i, k;
	    cin>>a>>b>>c;
	    for(i=1; i<=99; i++){
	        // check if each a, b, c mod i should not be equal to 0
	        if(a%i!=0 && b%i!=0 && c%i!=0){ 
	            k=i; //if true, store value of i in k
	            break; //terminate loop
	    }
	    }
	    cout<<k<<endl; //print value
	}
	return 0;
}
