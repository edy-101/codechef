#include <iostream>
using namespace std;
int main() {
	int t, n, a, b, timme;
	cin>>t;
	while(t--){
	    cin>>n>>a>>b;
	    if (n%2==0){
	        timme= ((n/2)*a) + ((n/2)*b);
	    }
	    
	    else {
	        timme= ((n/2)*a) + (((n/2)+1)*b);
	    }
	    
	    cout<<timme<<endl;
	}
	return 0;
}
