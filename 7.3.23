#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	// Your code here
     string s;
    cin >> s;
    if (s[0] == s[1] && s[1] == s[2] && s[0] == s[2]) {
        cout << "Won" << endl;
    } else {
        cout << "Lost" << endl;
    }
    return 0;
}
