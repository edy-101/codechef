#include <bits/stdc++.h>
using namespace std;
int main() {
	int t, a, b, c, d;
	cin>>t;
	while(t--){
	    cin>>a>>b>>c>>d;
	    int max1= max(a, b);
	    int max2= max(c, d);
	    int res= max1 + max2;
	    cout<<res<<endl;
	}
	return 0;
}
