#include <iostream>
using namespace std;

int main() {
    int x, y, t, money;
    cin>>t;
    while (t>0) {
        cin>>x>>y;
        money= (10*x) + (90*y);
        cout<<money<<endl;
        t--;
    }
	return 0;
}
