#include <bits/stdc++.h>
using namespace std;
int main() {
    int l, b, p, a;
    cin>>l;
    cin>>b;
    p = 2*(l+b);
    a = l*b;
    if (p>a) cout<<"Peri"<<endl<<p;
    else if (a>p) cout<<"Area"<<endl<<a;
    else cout<<"Eq"<<endl<<p;
	return 0;
}
