#include <bits/stdc++.h>
using namespace std;
int main() {
	int t, n, m;
	cin>>t;
	while(t--){
	    cin>>n>>m;
	    int run=m*36;
	    if(n<=run) cout<<"yes"<<endl;
	    else cout<<"no"<<endl;
	}
	return 0;
}
