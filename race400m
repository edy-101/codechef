#include <iostream>
using namespace std;
int main() {
	int t, x, y, z;
	cin>>t;
	while(t--){
	    cin>>x>>y>>z;
	    if(x<y && x<z) cout<<"alice"<<endl;
	    else if(y<z && y<x) cout<<"bob"<<endl;
	    else if (z<x && z<y) cout<<"charlie"<<endl;
	}
	return 0;
}
