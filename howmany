#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin>>n;
    string s=to_string(n);
    if (s.size()==1) cout<<"1";
    else if (s.size()==2) cout<<"2";
    else if (s.size()==3) cout<<"3";
    else cout<<"More than 3 digits";
	return 0;
}
