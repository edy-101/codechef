import math as m
t = int(input())
for _ in range(t):
    inp = input()
    x, n = map(int, inp.split())
    total = m.ceil(n/100)
    if x>total:
        print(0)
    else:
        need = total - x
        print(need)
