#include <iostream>
using namespace std;
int main() {
	int t, x, y, z;
	cin>>t;
	while(t--){
	    cin>>x>>y>>z;
	    int seats= x*10;
	    if (y>seats) cout<<z*seats<<endl;
	    else cout<<y*z<<endl;
	}
	return 0;
}
