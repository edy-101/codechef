#include <iostream>
using namespace std;
int main() {
	int t;
	cin>>t;
	while(t--){
	    int n, x;
	    cin>>n>>x;
	    int bill= n*x;
	    string count=to_string(bill);
	    int digit=count.length();
	    if(digit==5) cout<<"yes"<<endl;
	    else cout<<"no"<<endl;
	}
	return 0;
}
